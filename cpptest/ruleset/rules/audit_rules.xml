<group name="audit">
  <rule id="80800" level="1">
    <match>SYSCALL</match>
    <description>syscall audit</description>
  </rule>

  <rule id="80801" level="5">
    <if_matched_sid>80800</if_matched_sid>
    <match>success=no</match>
    <description>syscall failed</description>
  </rule>
</group>
